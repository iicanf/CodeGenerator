spring:
    application:
        name: iicanf-ad
    datasource:
        hikari:
            connection-test-query: SELECT 1 FROM DUAL
            minimum-idle: 10
            maximum-pool-size: 200
            pool-name: yourPoolName
            auto-commit: false
            connection-timeout: 5000
        driverClassName: com.mysql.cj.jdbc.Driver
        type: com.zaxxer.hikari.HikariDataSource
#        url: jdbc:mysql://10.1.49.25:64311/merchant-center?useUnicode=true&characterEncoding=UTF-8&useSSL=false&allowMultiQueries=true
        url: jdbc:mysql://10.1.82.12:3306/merchant-center2?useUnicode=true&characterEncoding=utf-8&useSSL=false
        username: root
        password: root@20180320

management:
    security:
        enabled: false

mybatis:
    mapper-locations: 'classpath:mapper/*.xml'

mapper:
    mappers:
        - tk.mybatis.mapper.common.Mapper
    notEmpty: true

